version: "2.1"
services:
  mqtt-broker:
    build: ./mqtt-broker
    restart: always
    ports:
      - 1883:1883
      - 9001:9001
  brightness-service:
    build: ./brightness-service
    devices:
      - /dev/ttyACM0